
function api.go.online {

    capacities.tell "Starting the nginx creation script"
    cd ../lib/tools/nginx && sudo ./create_nginx_service.sh -a "marvin.io" "../web" "$(whoami)" "both" "80" "443"
    capacities.tell "I'm done with that"








}
