function(){

    var load =  __platform__.lib.load;
    
    this.bubble = function (element) {

        //var ee = new Event('build')
        //debug = load('alert',ee);
        //debug("jusqu'ici tout va bien","hello",ee);

    }

}
